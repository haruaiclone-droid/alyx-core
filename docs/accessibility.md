# Accessibility

- `AccessibilityMetadata` and `Role` are defined in `alyx-ir`
- `AccessibilityPlan` in `alyx-plan` stores node/element-based entries with rectangles
- `event_plan.focus_order` aligns focus traversal metadata
