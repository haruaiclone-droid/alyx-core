# Web

- Export to static HTML from rendering plan
- Browser event encoding/decoding helpers in `alyx-web`
- Runtime event bridge script and endpoint (`/__alyx_event`) for localhost host flows
- `alyx-host::serve_http_with_runtime` for browser input passthrough in one process
- Accessibility summary helpers
